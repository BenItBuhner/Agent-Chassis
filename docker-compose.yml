services:
  agent-chassis:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./mcp_config.json:/app/mcp_config.json # Allow config hot-reloading
    restart: unless-stopped
